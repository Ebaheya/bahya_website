# Full (real) pipeline dependencies — BAHYA_AI_MODE=real.
#
# torch is intentionally NOT listed: the CUDA base image
# (pytorch/pytorch:2.4.1-cuda12.1, see ai/Dockerfile) already ships the
# CUDA-matched build. Re-installing it here risks pulling a wheel that doesn't
# match the base CUDA and breaks quantization. Versions are pinned (==) so the
# image build is reproducible; bump deliberately and re-test on GPU.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

transformers==4.44.2
peft==0.13.2
accelerate==0.34.2
bitsandbytes==0.43.3          # GPU 4-bit quantization (Fanar / tanusrich)
sentence-transformers==3.1.1  # RAG embeddings
chromadb==0.5.5              # RAG vector store
fasttext-wheel==0.9.2         # language / dialect id
huggingface_hub==0.25.2
numpy==1.26.4
